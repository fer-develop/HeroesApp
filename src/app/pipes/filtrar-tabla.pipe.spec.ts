import { FiltrarTablaPipe } from './filtrar-tabla.pipe';

describe('FiltrarTablaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarTablaPipe();
    expect(pipe).toBeTruthy();
  });
});
